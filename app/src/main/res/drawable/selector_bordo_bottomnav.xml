<!-- <?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:bottom="-5dp" android:right="-5dp" android:left="-5dp">
        <shape android:shape="rectangle" >
            <solid android:color="@color/background" />
            <stroke android:color="@color/white" android:width="1dp" />
        </shape>
    </item>
</selector>
-->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
<!-- Stato selezionato -->
<item android:bottom="-5dp" android:right="-5dp" android:left="-5dp">
    <shape android:shape="rectangle" >
        <solid android:color="@color/background" /> <!-- Colore del fondo -->
        <stroke android:color="@color/colorBorderBottomNav" android:width="1dp" /> <!-- Colore e spessore del bordo -->
    </shape>
</item>
</layer-list>